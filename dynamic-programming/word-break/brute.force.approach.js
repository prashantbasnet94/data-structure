function wordProblem(array, s) {

    /*

            we need to break string s in every way and check if the word exist in the dict

            1. How to break s in every way?
                iterate it through

                i
                l e e t c o d e
                i + each word in dic = generated word, see if it matches

            for (let i = 0; i < s.length; i++){
                for(let word of array){
                     if(s.substring(0, i + 1) === word && wordProblem(s.substring(i+1), array)){
                        return true
                     }
                }
            }
            // If no possible break found, return false.
            return false;
    */
}